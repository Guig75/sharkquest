ccess');void document.getElementById('battle').offsetWidth;document.getElementById('battle').classList.add('captureSuccess');$('#battleText').textContent=`${b.sp.name} capturé !`;setTimeout(()=>{closeBattle();refresh();toast('Capture ajoutée au SharkDex !')},950)}else{sfx('fail');haptic(45);$('#battleText').textContent='Il se libère !';if(!enemyHit())setActions(true)}},850)};
$('#runBtn').onclick=()=>{if(!W.battle)return;sfx('run');setActions(false);if(Math.random()<.82){$('#battleText').textContent='Vous vous éloignez avec Syrachi.';setTimeout(closeBattle,500)}else{$('#battleText').textContent='Le requin vous coupe la route !';setTimeout(()=>{if(!enemyHit())setActions(true)},450)}};
function closeBattle(){cancelAnimationFrame(rafB);W.battle=null;$('#battle').classList.add('hidden');$('#battle').classList.remove('boss');$('#encounterIntro').classList.remove('show');refresh();save();W.nextFin=performance.now()+5000}
function renderDex(){let g=$('#dexGrid');g.innerHTML='';sharks.forEach(sp=>{let seen=S.seen[sp.id]||S.caught[sp.id],n=S.caught[sp.id]||0,d=document.createElement('button');d.className='dexCard'+(seen?'':' lock');d.innerHTML=`<div class="dexIcon">${seen?'🦈':'❔'}</div><div><b>${seen?sp.name:'Espèce inconnue'}</b><small>${seen?`${sp.rare} • x${n}`:'Non découverte'}</small></div>`;d.onclick=()=>{$('#dexShark').textContent=seen?'🦈':'❔';$('#dexName').textContent=seen?sp.name:'Espèce inconnue';$('#dexInfo').textContent=seen?`${sp.rare} • ${sp.mood}. ${sp.desc} Capturé x${n}.`:'Explore les lagons pour découvrir cette espèce.'};g.appendChild(d)})}
$('#dexBtn').onclick=()=>{renderDex();$('#dex').classList.remove('hidden')};$('#closeDex').onclick=()=>$('#dex').classList.add('hidden');const soundBtn=$('#soundBtn');function syncSound(){soundBtn.textContent=soundOn?'🔊':'🔇';soundBtn.classList.toggle('muted',!soundOn)}soundBtn.onclick=()=>{soundOn=!soundOn;localStorage.setItem('sharkquest-sound',soundOn?'on':'off');syncSound();if(soundOn){ensureAudio();tone(520,.08,'sine',.03,100)}};syncSound();addEventListener('pointerdown',()=>ensureAudio(),{once:true});addEventListener('keydown',e=>W.keys[e.key]=1);addEventListener('keyup',e=>W.keys[e.key]=0);$$('.dpad button').forEach(b=>{let d=b.dataset.dir;b.onpointerdown=e=>{e.preventDefault();W.touch[d]=1};b.onpointerup=b.onpointercancel=b.onpointerout=e=>{e.preventDefault();W.touch[d]=0}});refresh();drawMap();
})();