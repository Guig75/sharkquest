ayBtn').disabled=false;$('#playBtn').textContent='Commencer avec '+S.char;save()});$('#playBtn').onclick=()=>startGame(true);if(S.char){$('#resumeBtn').classList.remove('hidden');$('#resumeBtn').onclick=()=>startGame(false);let b=$(`.char-card[data-char="${S.char}"]`);if(b){b.classList.add('selected');$('#playBtn').disabled=false;$('#playBtn').textContent='Nouvelle partie avec '+S.char}}
let bgImg=new Image(),rafB=0;
function bgFor(sp){const src=k=>imgs[k]?.src||'';if(sp.id==='white'||sp.id==='meg')return Math.random()<.5?src('e3'):src('e4');if(sp.id==='whale')return src('e1');if(sp.id==='mako'||sp.id==='hammer')return Math.random()<.5?src('e5'):src('e2');return Math.random()<.5?src('e1'):src('e2')}
function startBattle(sp,z){if(W.battle)return;W.battle={sp,z,calm:100,shield:false,dogUsed:false,start:performance.now(),attackUntil:0,splash:[]};S.seen[sp.id]=true;save();const battle=$('#battle');battle.classList.remove('hidden');battle.classList.toggle('boss',sp.id==='meg');$('#enemyName').textContent=sp.name;$('#enemyRarity').textContent=sp.rare.toUpperCase();$('#enemyTemper').textContent=sp.mood;$('#enemyPct').textContent='100%';$('#enemyBar').style.width='100%';$('#battlePortrait').src=portrait(S.char);$('#battleName').textContent=S.char+' + Syrachi';$('#battleStats').textContent=`❤️ ${S.hp} • 🔴 ${S.balls}`;$('#battleText').textContent=sp.id==='meg'?'L’eau se soulève… le Mégalodon apparaît !':`Un ${sp.name} surgit près de ${z.name.toLowerCase()} !`;bgImg.src=bgFor(sp);cancelAnimationFrame(rafB);drawBattle();setActions(false);const intro=$('#encounterIntro');$('#introTag').textContent=sp.id==='meg'?'ALERTE LÉGENDAIRE':sp.rare.toUpperCase();$('#introTitle').textContent=sp.name.toUpperCase();$('#introSub').textContent=sp.id==='meg'?'Quelque chose d’énorme remonte des profondeurs…':`Rencontre à ${z.name}`;intro.classList.add('show');sp.id==='meg'?sfx('boss'):sfx('encounter');haptic(sp.id==='meg'?[80,50,120]:35);setTimeout(()=>{intro.classList.remove('show');setActions(true)},sp.id==='meg'?1650:900)}
function drawBattle(){if(!W.battle)return;const c=bctx,w=W.vw,h=W.vh,bb=W.battle,sp=bb.sp,age=(performance.now()-bb.start)/1000;c.clearRect(0,0,w,h);if(bgImg.complete&&bgImg.naturalWidth){let sc=Math.max(w/bgImg.naturalWidth,h/bgImg.naturalHeight),dw=bgImg.naturalWidth*sc,dh=bgImg.naturalHeight*sc;c.globalAlpha=.72;c.drawImage(bgImg,(w-dw)/2,(h-dh)/2,dw,dh);c.globalAlpha=1}else{let g=c.createLinearGradient(0,0,0,h);g.addColorStop(0,'#75c8d2');g.addColorStop(1,'#0e4651');c.fillStyle=g;c.fillRect(0,0,w,h)}c.fillStyle='#03101624';c.fillRect(0,0,w,h);c.strokeStyle='#fff6';c.lineWidth=3;for(let i=0;i<10;i++){c.beginPath();c.ellipse((i*153+age*42)%w,h*.45+i*23+Math.sin(age*2+i)*6,75,13,0,0,Math.PI*2);c.stroke()}let x=w*.7+Math.sin(age*sp.spd)*w*.05,y=h*.43+Math.sin(age*1.8)*10,s=Math.min(w/1100,h/680)*(sp.id==='meg'?1.55:sp.id==='whale'?1.3:1);if(performance.now()<bb.attackUntil){let q=1-(bb.attackUntil-performance.now())/520;s*=1+Math.sin(q*Math.PI)*.5;x-=Math.sin(q*Math.PI)*w*.13}drawShark(c,x,y,sp,s,age);for(const p of bb.splash){p.r+=3;p.a-=.04;c.strokeStyle=`rgba(255,255,255,${Math.max(0,p.a)})`;c.beginPath();c.arc(p.x,p.y,p.r,0,Math.PI*2);c.stroke()}bb.splash=bb.splash.filter(p=>p.a>0);rafB=requestAnimationFrame(drawBattle)}
function drawShark(c,x,y,sp,s,t){c.save();c.translate(x,y);c.scale(s,s);c.rotate(Math.sin(t*1.2)*.025);c.fillStyle=sp.c;c.strokeStyle='#28383c';c.lineWidth=3;c.beginPath();c.ellipse(0,0,180,60,0,0,Math.PI*2);c.fill();c.stroke();c.save();c.translate(-160,0);c.rotate(Math.sin(t*4*sp.spd)*.18);c.beginPath();c.moveTo(0,0);c.lineTo(-85,-64);c.lineTo(-64,0);c.lineTo(-85,64);c.closePath();c.fill();c.stroke();c.restore();c.beginPath();c.moveTo(-12,-50);c.lineTo(34,-126);c.lineTo(63,-42);c.closePath();c.fill();c.beginPath();c.moveTo(10,40);c.lineTo(70,105);c.lineTo(90,34);c.closePath();c.fill();if(sp.id==='hammer'){c.fillRect(118,-54,76,28);c.beginPath();c.ellipse(156,-40,42,18,0,0,Math.PI*2);c.fill()}else{c.beginPath();c.ellipse(147,0,73,50,0,0,Math.PI*2);c.fill()}if(sp.id==='tiger'){c.strokeStyle='#443b31';c.lineWidth=7;for(let i=-80;i<90;i+=34){c.beginPath();c.moveTo(i,-44);c.lineTo(i+18,44);c.stroke()}}if(sp.id==='whale'){c.fillStyle='#d7eef0';for(let i=0;i<24;i++){c.beginPath();c.arc(-120+(i*47)%270,-36+((i*31)%72),3,0,Math.PI*2);c.fill()}}c.fillStyle='#dbe2e3';c.beginPath();c.ellipse(64,24,126,26,0,0,Math.PI*2);c.fill();c.fillStyle='#02090c';c.beginPath();c.arc(150,-16,5,0,Math.PI*2);c.fill();if(sp.id==='meg'){c.strokeStyle='#f0f3f0';for(let i=0;i<6;i++){c.beginPath();c.moveTo(148+i*7,20);c.lineTo(153+i*7,32);c.stroke()}}c.restore()}
function setActions(on){['observeBtn','captureBtn','dogBtn','runBtn'].forEach(id=>$('#'+id).disabled=!on);if(W.battle?.dogUsed)$('#dogBtn').disabled=true}
function enemyHit(){let b=W.battle,d=b.sp.pow+Math.floor(Math.random()*5);sfx('attack');haptic(b.sp.id==='meg'?[70,35,110]:[35,25,55]);if(b.shield){d=Math.ceil(d/2);b.shield=false}S.hp=Math.max(0,S.hp-d);$('#battleStats').textContent=`❤️ ${S.hp} • 🔴 ${S.balls}`;b.attackUntil=performance.now()+520;b.splash.push({x:W.vw*.65,y:W.vh*.45,r:15,a:.9},{x:W.vw*.72,y:W.vh*.5,r:10,a:.7});document.getElementById('battle').classList.remove('attack');void document.getElementById('battle').offsetWidth;document.getElementById('battle').classList.add('attack');save();if(S.hp<=0){$('#battleText').textContent='Syrachi te ramène au camp.';setActions(false);setTimeout(()=>{S.hp=100;S.x=560;S.y=1180;closeBattle()},900);return true}return false}
$('#observeBtn').onclick=()=>{let b=W.battle;if(!b)return;sfx('observe');haptic(18);setActions(false);b.calm=Math.max(5,b.calm-(18+Math.floor(Math.random()*18)));$('#enemyPct').textContent=b.calm+'%';$('#enemyBar').style.width=b.calm+'%';$('#battleText').textContent='Tu observes ses mouvements…';setTimeout(()=>{if(b.sp.id==='whale'&&Math.random()<.6){$('#battleText').textContent+=' Il reste paisible.';setActions(true)}else if(!enemyHit()){$('#battleText').textContent+=' Il riposte !';setActions(true)}},650)};
$('#dogBtn').onclick=()=>{let b=W.battle;if(!b||b.dogUsed)return;sfx('dog');haptic([20,25,20]);b.shield=true;b.dogUsed=true;$('#battleText').textContent='Syrachi détourne son attention.';$('#dogBtn').disabled=true};
$('#captureBtn').onclick=()=>{let b=W.battle;if(!b)return;if(S.balls<=0){$('#battleText').textContent='Plus de Pokéballs.';return}S.balls--;sfx('throw');haptic(18);$('#battleStats').textContent=`❤️ ${S.hp} • 🔴 ${S.balls}`;setActions(false);let fx=$('#ballFx');fx.classList.remove('go');void fx.offsetWidth;fx.classList.add('go');let chance=Math.min(.94,b.sp.base+(100-b.calm)/125+(b.dogUsed?.04:0));$('#battleText').textContent='Pokéball lancée…';setTimeout(()=>{if(Math.random()<chance){S.caught[b.sp.id]=(S.caught[b.sp.id]||0)+1;S.seen[b.sp.id]=true;save();sfx('success');haptic([30,25,30,25,70]);let f=$('#captureFlash');f.classList.remove('go');void f.offsetWidth;f.classList.add('go');document.getElementById('battle').classList.remove('captureSu